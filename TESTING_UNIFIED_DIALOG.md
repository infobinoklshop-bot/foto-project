# Инструкция по тестированию объединенного диалога управления параметрами

## Что было сделано

✅ Объединены два диалога в один:
- **"➕ Добавить ненормализованные значения"** (UnnormalizedValuesDialog.html)
- **"🔄 Сопоставить параметры товара"** (ParameterMappingDialog.html)

✅ Создан новый единый диалог:
- **UnifiedParameterDialog.html** - объединенный интерфейс с вкладками
- Светлая тема (белый/серый фон, синие акценты)
- Две вкладки: "📦 Товар" и "📋 Все ненормализованные значения"

✅ Обновлен код:
- Добавлена функция `showUnifiedParameterDialog()` в [06_specification_normalizer.js](06_specification_normalizer.js:1530)
- Обновлено меню в [99_menu.js](99_menu.js:60) - два пункта объединены в один
- Старые функции помечены как `@deprecated`

✅ Старые файлы сохранены:
- UnnormalizedValuesDialog.html.old
- ParameterMappingDialog.html.old

---

## Деплой в Google Apps Script

### Шаг 1: Загрузка файлов

```bash
# Убедитесь, что вы в директории проекта
cd "/Users/evgenijermakov/Documents/Фото"

# Загрузите изменения через clasp
clasp push
```

**Файлы, которые будут обновлены:**
- ✅ UnifiedParameterDialog.html (новый)
- ✅ 06_specification_normalizer.js (изменен)
- ✅ 99_menu.js (изменен)

**Файлы, которые НЕ будут загружены (помечены .old):**
- ⏸️ UnnormalizedValuesDialog.html.old
- ⏸️ ParameterMappingDialog.html.old

### Шаг 2: Проверка в Google Apps Script Editor

1. Откройте проект в браузере:
   ```bash
   clasp open
   ```

2. Проверьте, что файлы загружены:
   - UnifiedParameterDialog.html (новый файл)
   - 06_specification_normalizer.js (функция `showUnifiedParameterDialog` добавлена)
   - 99_menu.js (меню обновлено)

3. Проверьте отсутствие синтаксических ошибок (красных подчеркиваний)

---

## Тестирование функционала

### Подготовка к тестированию

1. **Откройте Google Sheets** с вашим проектом
2. **Обновите страницу** (Ctrl+R / Cmd+R)
3. Подождите загрузки меню **"🖼️ Фото"**

### Тест 1: Проверка меню

**Ожидаемый результат:**
```
🖼️ Фото
├── ...
├── 📖 Управление справочником параметров
├── 🔄 Управление параметрами  ← НОВЫЙ пункт (вместо двух старых)
├── 🆘 Справка
```

**Что проверить:**
- ✅ Старые пункты "➕ Добавить ненормализованные значения" и "🔄 Сопоставить параметры товара" УДАЛЕНЫ
- ✅ Новый пункт "🔄 Управление параметрами" присутствует

---

### Тест 2: Открытие диалога (без выбранного товара)

**Шаги:**
1. НЕ выбирайте ни один товар (чекбокс в колонке A)
2. Меню → "🔄 Управление параметрами"

**Ожидаемый результат:**
```
⚠️ Товар не выбран

Пожалуйста, выберите товар с помощью чекбокса
в первой колонке таблицы.

Это необходимо для вкладки "Товар".
Вкладка "Все ненормализованные значения" будет
доступна в любом случае.
```

**Что проверить:**
- ✅ Появляется предупреждение
- ✅ Диалог НЕ открывается

---

### Тест 3: Открытие диалога (с выбранным товаром)

**Шаги:**
1. **Выберите любой товар** - поставьте галочку в колонке A
2. Меню → "🔄 Управление параметрами"

**Ожидаемый результат:**
- ✅ Диалог открылся
- ✅ Заголовок: "🔄 Управление нормализацией параметров"
- ✅ Две вкладки:
  - **"📦 Товар"** (активная по умолчанию)
  - **"📋 Все ненормализованные значения"**
- ✅ Светлая тема (белый фон, серые карточки, синие акценты)

---

### Тест 4: Вкладка "📦 Товар"

**Шаги:**
1. Откройте диалог с выбранным товаром
2. Убедитесь, что вкладка "📦 Товар" активна

**Ожидаемый результат:**
- ✅ **Карточка товара** вверху:
  - Артикул: (значение)
  - Название: (значение)
  - Поставщик: VEBER/STURMAN
- ✅ **Секция "✅ Сопоставленные параметры"** (может быть свернута):
  - Список параметров с зеленой рамкой
  - Три колонки: "Поле поставщика" → "Параметр справочника" → "Нормализованное значение"
- ✅ **Секция "⚠️ Несопоставленные параметры"** (раскрыта):
  - Список параметров с красной рамкой
  - Поле автокомплита для сопоставления
  - Кнопка "Добавить синоним"

**Что проверить:**
- ✅ Данные товара загружаются корректно
- ✅ Matched/unmapped параметры отображаются
- ✅ Можно сворачивать/разворачивать секции (клик по заголовку)

---

### Тест 5: Автокомплит параметров (Вкладка "Товар")

**Шаги:**
1. Вкладка "📦 Товар"
2. Найдите несопоставленный параметр (красная рамка)
3. Начните вводить название параметра в поле автокомплита

**Ожидаемый результат:**
- ✅ После 2+ символов появляется выпадающий список
- ✅ Список фильтруется по введенным символам
- ✅ Совпадающие символы подсвечены (желтый фон)
- ✅ Клик по элементу → значение вставляется в поле

**Что проверить:**
- ✅ Поиск работает case-insensitive
- ✅ Максимум 10 результатов в списке
- ✅ Список закрывается при клике вне его

---

### Тест 6: Добавление синонима (Вкладка "Товар")

**Шаги:**
1. Вкладка "📦 Товар"
2. Выберите параметр из автокомплита для несопоставленного поля
3. Нажмите "Добавить синоним"

**Ожидаемый результат:**
- ✅ Параметр меняет цвет с красного на зеленый
- ✅ Показывается "✅ Сопоставлено"
- ✅ В футере счетчик: "Добавлено сопоставлений: 1"

**Что проверить:**
- ✅ Визуальная обратная связь работает
- ✅ Можно добавить несколько сопоставлений
- ✅ Счетчик увеличивается

---

### Тест 7: Кнопка "Обновить товар" (Вкладка "Товар")

**Шаги:**
1. Добавьте хотя бы одно сопоставление
2. Нажмите кнопку "🔄 Обновить товар" (синяя кнопка)
3. Подтвердите действие

**Ожидаемый результат:**
- ✅ Показывается прогресс: "Обновление товара..."
- ✅ Вызывается функция `applyLocalMappings(article, mappings)`
- ✅ После успеха: алерт "✅ Товар успешно обновлен!"
- ✅ Диалог закрывается
- ✅ Данные в таблице обновлены (колонка Q - SPECIFICATIONS_NORMALIZED)

**Что проверить:**
- ✅ Изменения применяются только к ОДНОМУ товару
- ✅ Справочник параметров НЕ изменяется
- ✅ Другие товары НЕ ре-нормализуются

---

### Тест 8: Кнопка "Обновить справочник" (Вкладка "Товар")

**Шаги:**
1. Добавьте хотя бы одно сопоставление
2. Нажмите кнопку "📖 Обновить справочник" (зеленая кнопка)
3. Прочитайте предупреждение и подтвердите

**Ожидаемый результат:**
- ✅ Показывается предупреждение:
  ```
  ⚠️ ВНИМАНИЕ!

  Это действие обновит справочник параметров
  и ре-нормализует ВСЕ товары в таблице.

  Изменений: X

  Процесс может занять несколько минут.

  Продолжить?
  ```
- ✅ После подтверждения: прогресс "Обновление справочника..."
- ✅ Вызывается функция `applyGlobalMappings(mappings)`
- ✅ После успеха: алерт "✅ Справочник успешно обновлен! Ре-нормализовано товаров: X"
- ✅ Диалог закрывается

**Что проверить:**
- ✅ Предупреждение о глобальных изменениях отображается
- ✅ Синонимы добавляются в лист "Справочник параметров"
- ✅ ВСЕ товары в таблице ре-нормализуются
- ✅ Процесс занимает время (несколько минут для большой таблицы)

---

### Тест 9: Переключение на вкладку "📋 Все ненормализованные значения"

**Шаги:**
1. Откройте диалог
2. Кликните на вкладку "📋 Все ненормализованные значения"

**Ожидаемый результат:**
- ✅ Контент вкладки "Товар" скрывается
- ✅ Показывается контент вкладки "Все ненормализованные значения"
- ✅ Вкладка подсвечивается синим
- ✅ В футере: "Режим: пакетная обработка ненормализованных значений"

---

### Тест 10: Список ненормализованных значений (Вкладка 2)

**Шаги:**
1. Вкладка "📋 Все ненормализованные значения"
2. Просмотрите список значений

**Ожидаемый результат:**

**Если есть ненормализованные значения:**
- ✅ Инфобокс вверху с инструкцией
- ✅ Поле поиска "🔍 Поиск по названию параметра..."
- ✅ Список карточек, каждая содержит:
  - Название параметра (жирный шрифт)
  - Бейдж "Не обработано" (синий)
  - Сырое значение (красный фон)
  - Список допустимых значений (зеленый фон)
  - Затронутые товары (артикулы через запятую)
  - Три радио-кнопки с режимами действия
  - Кнопки "Применить" и "Пропустить"

**Если нет ненормализованных значений:**
- ✅ Зеленый бокс: "✅ Отлично! Нет ненормализованных значений."

---

### Тест 11: Поиск по параметрам (Вкладка 2)

**Шаги:**
1. Вкладка "📋 Все ненормализованные значения"
2. Введите часть названия параметра в поле поиска

**Ожидаемый результат:**
- ✅ Список фильтруется в реальном времени
- ✅ Показываются только параметры, содержащие запрос
- ✅ Скрытые карточки имеют `display: none`

---

### Тест 12: Режим "Добавить новое значение" (Вкладка 2)

**Шаги:**
1. Выберите любое ненормализованное значение
2. Выберите режим "Добавить новое значение" (первая радио-кнопка)
3. Нажмите "Применить"

**Ожидаемый результат:**
- ✅ Карточка затухает (opacity: 0.6)
- ✅ Бейдж меняется на "✅ Обработано" (зеленый)
- ✅ Кнопка "Применить" становится неактивной: "✅ Применено"
- ✅ В футере счетчик: "Обработано значений: 1 из X"
- ✅ Изменение сохранено в массив `unnormalizedChanges`

---

### Тест 13: Режим "Выбрать существующее" (Вкладка 2)

**Шаги:**
1. Выберите другое ненормализованное значение
2. Выберите режим "Выбрать существующее" (вторая радио-кнопка)
3. Появляется выпадающий список с допустимыми значениями
4. Выберите одно из значений
5. Нажмите "Применить"

**Ожидаемый результат:**
- ✅ При выборе радио-кнопки показывается `<select>` с допустимыми значениями
- ✅ После "Применить" карточка затухает
- ✅ Бейдж "✅ Обработано"
- ✅ Счетчик увеличивается

---

### Тест 14: Режим "Переназначить параметр" (Вкладка 2)

**Шаги:**
1. Выберите третье ненормализованное значение
2. Выберите режим "Переназначить параметр" (третья радио-кнопка)
3. Начните вводить название другого параметра
4. Выберите параметр из автокомплита
5. Нажмите "Применить"

**Ожидаемый результат:**
- ✅ При выборе радио-кнопки показывается поле ввода с автокомплитом
- ✅ Автокомплит работает (фильтр по параметрам справочника)
- ✅ После выбора и "Применить" карточка затухает
- ✅ Счетчик увеличивается

---

### Тест 15: Кнопка "Применить все изменения" (Вкладка 2)

**Шаги:**
1. Обработайте несколько ненормализованных значений (любыми режимами)
2. Нажмите "✅ Применить все изменения" (зеленая кнопка внизу)
3. Подтвердите действие

**Ожидаемый результат:**
- ✅ Подтверждение: "Применить X изменений и ре-нормализовать затронутые товары?"
- ✅ Показывается прогресс: "Применение изменений и ре-нормализация товаров..."
- ✅ Вызывается функция `applyUnnormalizedValuesChanges(unnormalizedChanges)`
- ✅ После успеха: алерт "✅ Изменения успешно применены! Обработано: X, Ре-нормализовано товаров: Y"
- ✅ Диалог закрывается

**Что проверить:**
- ✅ Только обработанные значения применяются
- ✅ Затронутые товары ре-нормализуются
- ✅ Справочник параметров обновлен
- ✅ UNNORMALIZED_VALUES storage очищен (или обновлен)

---

### Тест 16: Кнопка "Пропустить" (Вкладка 2)

**Шаги:**
1. Выберите любое ненормализованное значение
2. Нажмите "Пропустить" (серая кнопка)

**Ожидаемый результат:**
- ✅ Карточка затухает (opacity: 0.3)
- ✅ Бейдж меняется на "Пропущено" (серый)
- ✅ Изменение НЕ добавляется в массив `unnormalizedChanges`
- ✅ Счетчик НЕ увеличивается

---

### Тест 17: Переключение между вкладками без потери данных

**Шаги:**
1. Вкладка "📦 Товар" → добавьте сопоставление (но НЕ нажимайте "Обновить")
2. Переключитесь на вкладку "📋 Все ненормализованные значения"
3. Обработайте несколько значений (но НЕ нажимайте "Применить все")
4. Вернитесь на вкладку "📦 Товар"

**Ожидаемый результат:**
- ✅ Данные на вкладке "Товар" сохранены (добавленные сопоставления видны)
- ✅ Переключитесь обратно → данные на вкладке 2 тоже сохранены

**Что проверить:**
- ✅ Состояние каждой вкладки сохраняется при переключении
- ✅ Массивы `productMappings` и `unnormalizedChanges` не сбрасываются

---

### Тест 18: Кнопка "Закрыть" в футере

**Шаги:**
1. Откройте диалог
2. Нажмите кнопку "Закрыть" в правом нижнем углу (футер)

**Ожидаемый результат:**
- ✅ Диалог закрывается
- ✅ Google Sheets возвращается в обычный режим

---

### Тест 19: Обработка ошибок

**Шаги:**
1. Вкладка "📦 Товар" → попробуйте нажать "Обновить товар" БЕЗ добавления сопоставлений
2. Вкладка 2 → попробуйте нажать "Применить все изменения" БЕЗ обработки значений

**Ожидаемый результат:**
- ✅ Вкладка 1: Алерт "Нет изменений для применения"
- ✅ Вкладка 2: Алерт "Нет изменений для применения. Выберите действия для значений."
- ✅ Диалог НЕ закрывается
- ✅ Никакие функции сервера НЕ вызываются

---

## Проверка обратной совместимости

### Старые функции все еще работают (для теста)

Хотя старые функции помечены как `@deprecated`, они должны работать для обратной совместимости:

1. Откройте **Apps Script Editor**
2. Запустите вручную:
   ```javascript
   showUnnormalizedValuesDialog()
   showParameterMappingDialog()
   ```

**Ожидаемый результат:**
- ✅ Старые диалоги все еще открываются (используют .old файлы)
- ✅ Функционал работает как раньше

**НО:** В меню эти пункты больше НЕ отображаются.

---

## Возможные проблемы и решения

### Проблема 1: Диалог не открывается

**Симптомы:**
- Клик по меню ничего не делает
- Ошибка в консоли браузера

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок JavaScript
3. Убедитесь, что `clasp push` прошел успешно
4. Проверьте, что файл `UnifiedParameterDialog.html` загружен в Apps Script

---

### Проблема 2: Автокомплит не работает

**Симптомы:**
- Ввод текста не показывает выпадающий список
- Список пустой

**Решение:**
1. Проверьте, что функция `getAllParameterNames()` возвращает данные
2. В консоли браузера проверьте массив `allParameters`:
   ```javascript
   console.log(allParameters);
   ```
3. Убедитесь, что вводите 2+ символа (минимум для показа списка)

---

### Проблема 3: Данные не загружаются

**Симптомы:**
- Спиннер крутится бесконечно
- Ошибка "Ошибка загрузки данных"

**Решение:**
1. Откройте **Apps Script Editor** → **Выполнения** (Executions)
2. Посмотрите последние выполнения и ошибки
3. Проверьте, что товар выбран чекбоксом перед открытием диалога
4. Проверьте, что колонка P (SPECIFICATIONS_RAW) содержит валидный JSON

---

### Проблема 4: "Применить изменения" не работает

**Симптомы:**
- Нажатие кнопки ничего не делает
- Ошибка в алерте

**Решение:**
1. Проверьте, что обработали хотя бы одно значение
2. Убедитесь, что массив `unnormalizedChanges` не пустой:
   ```javascript
   console.log(unnormalizedChanges);
   ```
3. Проверьте логи выполнения в Apps Script

---

## Откат к старым диалогам (если нужно)

Если новый диалог не работает и нужно срочно вернуться к старым:

```bash
# Переименуйте файлы обратно
mv "UnnormalizedValuesDialog.html.old" "UnnormalizedValuesDialog.html"
mv "ParameterMappingDialog.html.old" "ParameterMappingDialog.html"

# Удалите новый файл (или переименуйте)
mv "UnifiedParameterDialog.html" "UnifiedParameterDialog.html.disabled"

# Верните старые пункты меню в 99_menu.js
# (замените строку 60 на оригинальные две строки)

# Загрузите изменения
clasp push
```

---

## Контрольный чек-лист после деплоя

- [ ] Файлы загружены через `clasp push`
- [ ] Меню обновилось (два пункта заменены на один)
- [ ] Диалог открывается при выборе товара
- [ ] Вкладка "Товар" работает: автокомплит, добавление синонимов
- [ ] Вкладка "Все ненормализованные" работает: три режима, поиск
- [ ] Кнопки "Обновить товар" и "Обновить справочник" работают
- [ ] Кнопка "Применить все изменения" работает
- [ ] Переключение между вкладками не теряет данные
- [ ] Светлая тема применена корректно
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах Apps Script

---

## Контакты для вопросов

Если возникли вопросы или проблемы:
- Проверьте логи в Apps Script: **Выполнения** (Executions)
- Проверьте консоль браузера (F12)
- Откройте issue в репозитории проекта

---

**Дата создания:** 2025-11-14
**Версия:** 1.0
**Автор:** Claude Code Assistant
